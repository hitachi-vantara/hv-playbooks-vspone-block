# ============================================================
# Inventory: vcsa_deploy/vmware_inventory.yml
# Description:
#   Inventory for VCSA 9.x deployment and configuration.
#   All sensitive values (ESXi credentials, VCSA passwords,
#   IPs, SSO settings, datastore) are stored in Ansible Vault:
#     ansible_vault_vmware_var.yml
# ============================================================
---
all:
  hosts:
    localhost:
      ansible_connection: local
